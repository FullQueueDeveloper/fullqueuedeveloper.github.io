---
import BaseLayout from '@layouts/BaseLayout.astro';
import tools from '@data/tools.json';
import equipment from '@data/equipment.json';
import H3 from '@elements/H3.astro';
import P from '@elements/P.astro';
import List from '@elements/List.astro';
import ListItem from '@elements/ListItem.astro';
import Link from '@elements/Link.astro';
import Section from '@elements/Section.astro';
import H2 from '@elements/H2.astro';
let keyboardEpisodes = await Astro.glob('./keyboard/*.md');

---
<BaseLayout title="Keyboard Story">

  <h1 class="container mx-auto px-8 pt-4 my-4 sm:rounded max-w-prose">
    <span class="text-white text-3xl font-bold">Keyboard Story</span>
  </h1>

  <Section>
    <H3>There's so many keyboards out there.</H3>

    <P>My journey started in Spring of 2021, when my wrists started hurting. For other folks out there with wrists, I would like to give you a map of the keyboards that I encountered. Hopefully this series of blog posts will help your wrists as well.
    </P>
  </Section>


  <Section>
    <H2>Episodes</H2>

    <List>
      {keyboardEpisodes.map(item => (
        <ListItem><Link href={item.url}>{item.frontmatter.title}</Link></ListItem>
      ))}
    </List>
  </Section>

  <Section>
    <H2>Current equipment</H2>

    <List>
      {equipment.map(item => (
        <ListItem><Link href={item.url}>{item.title}</Link></ListItem>
      ))}
    </List>
  </Section>

</BaseLayout>
